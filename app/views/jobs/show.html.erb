<p id="notice"><%= notice %></p>

<table class = "striped">
  <tbody>
    <tr>
      <td>Name:</td>
      <td><%= @job.name %></td>
    </tr>
    <tr>
      <td>Description:</td>
      <td><%= @job.description %></td>
    </tr>
    <tr>
      <td>Active:</td>
      <td><%= @job.active%></td>
    </tr>
    
  </tbody>
</table>
<br>

<%= link_to 'Edit', edit_job_path(@job), :class => "btn-large waves-effect waves-light black-text yellow accent-3" %>

<%= link_to 'Back', jobs_path, :class => "btn-large waves-effect waves-light blue" %>