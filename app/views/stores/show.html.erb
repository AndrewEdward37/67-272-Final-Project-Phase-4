<p id="notice"><%= notice %></p>

<table class = "striped">
  <tbody>
    <tr>
      <td>Name:</td>
      <td><%= @store.name %></td>
    </tr>
    <tr>
      <td>Street:</td>
      <td><%= @store.street %></td>
    </tr>
    <tr>
      <td>City:</td>
      <td><%= @store.city %></td>
    </tr>
    <tr>
      <td>State:</td>
      <td><%= @store.state %></td>
    </tr>
    <tr>
      <td>Zip:</td>
      <td><%= @store.name %></td>
    </tr>
    <tr>
      <td>Phone:</td>
      <td><%= @store.name %></td>
    </tr>
    <tr>
      <td>Active:</td>
      <td><%= @store.active%></td>
    </tr>
    
  </tbody>
</table>
<br>

<%= link_to 'Edit', edit_store_path(@store), :class => "btn-large waves-effect waves-light black-text yellow accent-3" %>

<%= link_to 'Back', stores_path, :class => "btn-large waves-effect waves-light blue" %>