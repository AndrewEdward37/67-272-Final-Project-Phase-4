<!-- ------------------------------------------------------------------  -->

<p id="notice"><%= notice %></p>

<table class = "striped">
  <tbody>
    <tr>
      <td>Start date:</td>
      <td><%= @assignment.start_date %></td>
    </tr>
    <tr>
      <td>End date:</td>
      <td><%= @assignment.end_date %></td>
    </tr>
    <tr>
      <td>Pay level:</td>
      <td><%= @assignment.pay_level %></td>
    </tr>
    <tr>
      <td>Store:</td>
      <td><%= @assignment.store_id %></td>
    </tr>
    <tr>
      <td>Employee:</td>
      <td><%= @assignment.employee_id %></td>
    </tr>
  </tbody>
</table>
<br>
<!-- ------------------------------------------------------------------  -->

<%= link_to 'Edit', edit_assignment_path(@assignment), :class => "btn-large waves-effect waves-light black-text yellow accent-3" %>

<%= link_to 'Back', assignments_path, :class => "btn-large waves-effect waves-light blue" %>